# algoritmos-
#include <stdio.h>
#include <conio.h>
#include <math.h>

main(){

    char c;
    scanf("%c", &c);

    if(c == 'p'){

        printf("Voce e paulista");

        }

    if(c == 'r'){

        printf("Voce e carioca");

    }

    if(c == 'm'){

        printf("Voce e mineiro");

    }

    if((c != 'p') && (c != 'r') && (c != 'm')){

        printf("Outro estado");
    }
}
